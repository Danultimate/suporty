upstream supporty {
    server app:8000;
    keepalive 32;
}

# Rate limiting zone
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/m;

server {
    listen 80;
    server_name _;

    # Redirect HTTP → HTTPS (enable after setting up TLS)
    # return 301 https://$host$request_uri;

    location /api/ {
        limit_req zone=api burst=10 nodelay;

        proxy_pass         http://supporty;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";

        # Timeouts for long-running LLM calls
        proxy_read_timeout  120s;
        proxy_send_timeout  120s;

        # Security headers
        add_header X-Content-Type-Options  "nosniff"       always;
        add_header X-Frame-Options         "DENY"          always;
        add_header X-XSS-Protection        "1; mode=block" always;
        add_header Referrer-Policy         "no-referrer"   always;
    }

    location /health {
        proxy_pass http://supporty/api/v1/health;
        access_log off;
    }

    # Block everything else
    location / {
        return 404;
    }
}

# HTTPS server (uncomment and add cert paths after cert provisioning)
# server {
#     listen 443 ssl http2;
#     server_name yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     include /etc/nginx/snippets/ssl-params.conf;
#
#     location /api/ { ... }
# }
